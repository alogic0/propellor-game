-- | This module is rewritten by the installer to record the user's input,
-- so don't put anything else in here.
module Installer.UserInput (userInput) where

import Installer.Types

userInput :: UserInput
userInput = noUserInput
